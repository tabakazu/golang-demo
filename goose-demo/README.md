# goose-demo

## Use container

```bash
$ docker-compose build
$ docker-compose run --rm goose bash
docker > goose status
docker > goose create create_users sql
docker > goose up
docker > goose down
```
